name: Deploy to Railway

on:
  workflow_call:
    secrets:
      RAILWAY_TOKEN:
        description: 'Railway account token for deployment'
        required: true

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest

    steps:
      - name: Install Railway CLI
        run: npm install -g @railway/cli

      - name: Deploy to Railway
        run: railway redeploy --service artlas-api --yes
        env:
          RAILWAY_API_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
